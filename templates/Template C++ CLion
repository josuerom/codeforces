#pragma GCC optimize("O3,unroll-loops,unsafe-loop-optimizations,inline-functions,-march=native")

#include <bits/stdc++.h>             //          ██╗░█████╗░░██████╗██╗░░░██╗███████╗██████╗░░█████╗░███╗░░░███╗
using namespace std;                 //          ██║██╔══██╗██╔════╝██║░░░██║██╔════╝██╔══██╗██╔══██╗████╗░████║
                                     //          ██║██║░░██║╚█████╗░██║░░░██║█████╗░░██████╔╝██║░░██║██╔████╔██║
#ifdef josuerom                      //     ██╗░░██║██║░░██║░╚═══██╗██║░░░██║██╔══╝░░██╔══██╗██║░░██║██║╚██╔╝██║
#include "debug.h"                   //     ╚█████╔╝╚█████╔╝██████╔╝╚██████╔╝███████╗██║░░██║╚█████╔╝██║░╚═╝░██║
#else                                //      ╚════╝░░╚════╝░╚═════╝░░╚═════╝░╚══════╝╚═╝░░╚═╝░╚════╝░╚═╝░░░░░╚═╝
#define debug(...) 42                //                         created: {DAY}/{MONTH}/23 {HOUR}:{MINUTE}:{SECOND}
#endif

#define readArray(x)        void template<typename T> void read(xtor<T>&(x)){int n=(x).size();for(auto&e:x) cin >> e;}
#define io_speed()          ios::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr); cerr.tie(nullptr);
#define decimals(x)         cout.setf(ios::fixed, ios::floatfield); cout.precision(x);
#define uniq(x)             sort(all(x)); (x).erase(unique(all(x)),(x).end())
#define TC                  int tt; cin >> tt; for (int i = 1; i <= tt; i++)
#define FORR(i, x)          for(int i = (int)(x).size()-1; i >= 0; i--)
#define mod(a, b, mod)      ((((a) % (mod)) + ((b) % (mod))) % (mod))
#define FORI(i, b, c)       for(int (i) = (b); (i) < (c); (i)++)
#define veredict(x)         cout << ((x) ? "YES" : "NO") << '\n'
#define binary_search(x, y) sort(x); binary_search(all(x), (y))
#define memset(a, v)        memset((a),(v),sizeof(a))
#define rall(x)             (x).rbegin(),(x).rend()
#define PI                  3.14159265358979323846L
#define EULER               2.71828182845904523536L
#define count_bits(x)       __builtin_popcountll(x)
#define lbd(x, y)           lower_bound(all(x), y)
#define ubd(x, y)           upper_bound(all(x), y)
#define ull                 unsigned long long int
#define all(x)              (x).begin(),(x).end()
#define FORE(e, x)          for (auto &(e): (x))
#define log(x)              __builtin_clzll(x)
#define is_ordered(x)       is_sorted(all(x))
#define mcm(a, b)           (a*b)/gcd(a, b)
#define mcd(a, b)           (int) gcd(a, b)
#define rsort(x)            short(rall(x))
#define ll                  long long int
#define sort(x)             sort(all(x))
#define eb                  emplace_back
#define mxe                 max_element
#define mne                 min_element
#define vi                  vector<int>
#define pf                  push_front
#define pb                  push_back
#define lowbit(x)           (x & (-x))
#define MOD                 1000000007
#define mb                  make_pair
#define sbl(x, b)           (x << b)
#define sbr(x, b)           (x >> b)
#define se                  second
#define fi                  first

const int MAX = 1e6+7;

void solve() {
}

int main() {
   io_speed();
   TC {
      #ifdef josuerom
         cout << "----- Case #" << i << " -----\n";
         solve();
      #else
         solve();
      #endif
   }
}
